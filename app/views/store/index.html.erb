<% if notice %>
<aside id="notice"> <%= notice  %></aside>
<% end %>

<h1> Catalog </h1>

<ul class="catalog">
<% cache @products do %>
 <% @products.each do |product| %>
  <% cache product do %>
  <li>
    <%= image_tag(product.image_url , style: "width:5em; height:5em;") %>
    <p>
    <p style="font-weight:bold;"><%= product.title %></p> <br>
<%
=begin%>
 <%= sanitize(product.description) %> </p> 
<%
=end%>
<div class="price">  <%= number_to_currency(product.price) %>
<%= button_to 'Add to Cart' , line_items_path(product_id: product) , remote: true %>
</div>

  </li>
  <% end %>
 <% end %>
 <% end %>

</ul>